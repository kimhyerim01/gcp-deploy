# 서버 포트 설정
server:
  port: 8080
  address: 0.0.0.0

# database 연동 설정
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://34.60.104.129:3306/sys
    username: root

  thymeleaf:
    cache: false



logging:
  level:
    root=DEBUG:

jwt:
  secret: my-super-secret-jwt-signing-key
